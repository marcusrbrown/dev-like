---
"dev-like": patch
---

Quality gates: provenance link-rot checker (`scripts/check-links.mjs` + weekly Link Check CI with auto-filed issues), opt-out/profile-request issue forms, PR template with consent-tier checklist, description-trigger evals (60/60 stable), paired LLM eval harness (`bun run eval:paired`).
