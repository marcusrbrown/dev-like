---
"dev-like": minor
---

Deterministic skill generation: `scripts/generate-skill.mjs` (zero-dep CLI with `--out`/`--check`/`--registry`), generation templates in `skills/dev-like/assets/`, prebuilt `registry/every/skill/develop-like-every/` artifact, and a 9-test suite covering determinism, provenance closure, and snapshot drift.
