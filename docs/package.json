{
  "name": "@dev-like/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run generate && astro dev",
    "build": "bun run generate && astro build && bun run check:internal-links",
    "preview": "astro preview",
    "generate": "bun scripts/generate-registry-pages.ts",
    "check:internal-links": "bun scripts/check-internal-links.ts",
    "test": "bun test tests/*.test.ts"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.0",
    "@astrojs/starlight": "^0.41.0",
    "astro": "^7.0.0",
    "astro-og-canvas": "^0.11.0",
    "rehype-mermaid": "^3.0.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "canvaskit-wasm": "0.41.1",
    "playwright": "1.61.1",
    "remark-gfm": "4.0.1"
  }
}
