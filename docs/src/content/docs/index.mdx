---
title: dev-like
description: Steal the workflow, not the code.
template: splash
hero:
  tagline: Steal the workflow, not the code. <code>/dev-like <em>target</em></code> and your agent develops like the shops you admire, with receipts.
  actions:
    - text: npx skills add marcusrbrown/dev-like
      link: https://github.com/marcusrbrown/dev-like#install
      attrs:
        "data-cta": "primary"
        "data-umami-event": "install-primary"
---

import BeforeAfter from '../../components/BeforeAfter.astro';
import CopyCommand from '../../components/CopyCommand.astro';

<div class="landing-content">
  <section class="proof-section">
    <BeforeAfter />
    <div class="transcript-link">
      <a href="https://github.com/marcusrbrown/dev-like/blob/main/docs/demo/every-dryrun-2026-07-11.md" target="_blank" rel="noopener noreferrer">View the verified raw transcript on GitHub</a>
    </div>
  </section>

  <section class="grid-layout">
    <div class="install-paths">
      <h2>Alternate Install Paths</h2>
      <p>For Claude Code plugins:</p>
      <CopyCommand command="/plugin marketplace add marcusrbrown/dev-like" event="install-plugin-marketplace" />
      <CopyCommand command="/plugin install dev-like" event="install-plugin-direct" />
      
      <p>For cached, headless installations:</p>
      <CopyCommand command="npx dev-like <slug>" event="install-cli-cached" />
    </div>

    <div class="first-run">
      <h2>First Run & Generation</h2>
      <p>Invoke via your agent harness. If uncached, it runs live OSINT and builds a cited profile.</p>
      <div class="command-block">/dev-like <em>target</em></div>
      <p>Artifacts land safely isolated in <code>.agents/skills/develop-like-target/</code>.</p>
    </div>
  </section>

  <section class="context-section">
    <h2>Registry & Context</h2>
    <p>Browse the compiled, cited <a href="/dev-like/registry/">profiles in the registry</a>. Want a shop profiled? <a href="https://github.com/marcusrbrown/dev-like/issues/new?template=profile-request.yml" data-umami-event="request-profile">Request it</a>.</p>
    <p>Review the <a href="/dev-like/ethics/">ethics and consent guidelines</a> for public-source scoping and opt-out details.</p>
  </section>
</div>